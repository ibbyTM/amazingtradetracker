# Anthropic API key for the Trueline AI coach.
# Get one at https://platform.claude.com — then put the real key in .env.local
# (gitignored) so it never lands in version control.
VITE_ANTHROPIC_API_KEY=your_key_here
